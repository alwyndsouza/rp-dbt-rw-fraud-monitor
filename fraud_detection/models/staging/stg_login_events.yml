version: 2

models:
  - name: stg_login_events
    description: Parsed login events with timestamp cast
    columns:
      - name: event_id
        tests:
          - not_null
          - unique
      - name: customer_id
        tests:
          - not_null
      - name: occurred_at
        tests:
          - not_null
